---
title: API Reference
description: Complete Python SDK API reference.
---

# Python SDK - API Reference

## SolVec

```python
class SolVec:
    def __init__(self, network="devnet", wallet=None, wallet_key=None, rpc_url=None): ...
    def collection(self, name: str, dimensions: int, metric: str = "cosine") -> Collection: ...
```

## Collection

```python
class Collection:
    def upsert(self, vectors: list[dict]) -> None: ...
    def query(self, vector: list[float], top_k: int, min_score: float = 0.0, include_values: bool = False) -> list[QueryResult]: ...
    def delete(self, ids: str | list[str]) -> None: ...
    def fetch(self, ids: str | list[str]) -> list[Vector]: ...
    def stats(self) -> CollectionStats: ...
    def verify(self) -> VerifyResult: ...
```

## Exceptions

```python
class DimensionMismatchError(VecLabsError): ...
class InvalidIDError(VecLabsError): ...
class CollectionNotFoundError(VecLabsError): ...
class SolanaRPCError(VecLabsError): ...
```
